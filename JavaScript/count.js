function count(number) {
    for (let i = 0; i <= number; i++) {
        console.log(i)
    }
}

// for (run before the loop begins | constion that determines whether the loop runs | last thing to happen on each run of loop) 

count(50)